/**
 * Vite-compatible entry point for the Kiri:Moto minion (worker pool) worker.
 * This file exists so Vite can statically analyze the worker import.
 */
import '../../vendor/kiri-engine/src/kiri/run/minion.js';
